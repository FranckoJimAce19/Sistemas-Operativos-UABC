/**
 * @file  exit_cmd.c
 * @brief Comando exit — termina el interprete de comandos.
 */
#include "exit_cmd.h"

/**
 * @brief Retorna 0 para indicar al main que debe salir del bucle.
 */
int exitCmd(void) {
    return 0;
}
