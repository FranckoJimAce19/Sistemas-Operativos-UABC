/**
 * @file  exit_cmd.h
 * @brief Comando exit — termina el interprete de comandos.
 */
#ifndef EXIT_CMD_H
#define EXIT_CMD_H

/**
 * @brief Termina el interprete de comandos.
 *        Retorna 0 para indicar al main que debe salir del bucle.
 * @return 0 siempre.
 */
int exitCmd(void);

#endif /* EXIT_CMD_H */
